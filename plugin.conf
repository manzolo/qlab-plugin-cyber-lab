{
  "name": "cyber-lab",
  "description": "Attack/defense lab: an attacker VM and a defender VM on an internal LAN, where the attack IS the test — fail2ban bans are proven, not assumed",
  "version": "0.4"
}
